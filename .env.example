# === Infra-provided (do NOT add to secrets/production.enc.yaml) ===
# DATABASE_URL is injected by the server from infra SOPS secrets.
# Only set this locally for development.
DATABASE_URL=postgres://user:password@localhost:5432/myapp

# === App-owned secrets (add to secrets/production.enc.yaml) ===
# These are encrypted in this repo and pulled to the server as an OCI artifact.
# Add your app's secrets below:

# SESSION_SECRET=change-me-to-random-string
# API_KEY=your-api-key

# === Non-secret config ===
# BASE_URL=https://myapp.theor.net
